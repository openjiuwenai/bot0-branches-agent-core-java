# com.openjiuwen.retrieval.workflow.component.resource.KnowledgeRetrievalInput

## 类 KnowledgeRetrievalInput

```java
public class KnowledgeRetrievalInput
```

知识检索输入模型，承载查询文本。

## 字段

| 签名 | 说明 |
| --- | --- |
| `private String query` | 检索查询文本。 |

## 方法

| 签名 | 说明 |
| --- | --- |
| `public static KnowledgeRetrievalInput fromMap(Map<String, Object> inputs)` | 根据 `Map` 构造 `KnowledgeRetrievalInput` 实例。 |

## 说明

- 该类型使用 Lombok 生成部分访问器或构造方法，文档仅记录源码中显式定义的字段与方法。
