# com.openjiuwen.retrieval.common.Triple

## 类 Triple

```java
public class Triple
```

知识图谱三元组模型，保存 `subject`、`predicate`、`object`、可选置信度与元数据。

## 说明

- `subject`、`predicate`、`object` 均不能为空。
- `GraphKnowledgeBase` 图模式索引与检索依赖该模型。
